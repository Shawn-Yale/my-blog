---
title: P3199【HNOI2009】最小圈
date: 2025-08-03 16:41:27
tags:
  - 01分数规划
  - 二分答案
  - SPFA
  - 提高+/省选−
categories:
  - 算法刷题
mathjax: true
---

**[题目链接](https://www.luogu.com.cn/problem/P3199)**

每条边的边权为 $w_i$，求一个环 $C$ 使得 $\displaystyle \frac{\sum_{i \in C} w_i}{\sum_{i \in C} 1}$ 最小。

二分一个答案 $x$，把 $w_i - x$ 作为边权，那么最小环就是最小值。判断最小值是否 $< 0$，即判断是否存在负环。用 $SPFA$ 判负环即可。

这边使用 $BFS-SPFA$ 的通用做法，但是判负环（特别是全源）用 $DFS-SPFA$ 更快。可看 [A07 01分数规划——b站 董晓算法](https://www.bilibili.com/video/BV1N14y1w7us/?spm_id_from=333.1365.top_right_bar_window_history.content.click&vd_source=f0489718ccab992000c983a006bde4a5)。

时间复杂度：$\mathcal O(nm*log(2e17))$

```cpp
#include <bits/stdc++.h>
using namespace std;
#define endl '\n'
#define int long long
const int N = 3e3 + 10, M = 1.3e4 + 10, inf = 0x3f3f3f3f, INF = 1e18;

int n, m;
int h[N], e[M], ne[M], idx;
double w[M], dist[N];
int vis[N], cnt[N];

void add(int a, int b, double c){
    w[idx] = c;
    e[idx] = b;
    ne[idx] = h[a];
    h[a] = idx++;
}

bool check(double x){
    for(int i = 1; i <= n; i++)
        dist[i] = INF, vis[i] = 0, cnt[i] = 0;
    vis[0] = 1;
    queue<int> q;
    q.push(0);
    while(!q.empty()){
        int u = q.front(); q.pop();
        vis[u] = 0;
        for(int i = h[u]; ~i; i = ne[i]){
            int v = e[i];
            if(dist[v] > dist[u] + w[i] - x){
                dist[v] = dist[u] + w[i] - x;
                cnt[v] = cnt[u] + 1;
                if(cnt[v] >= n + 1) return true;
                // 注意本题用了超级源点，所以每个点都会多松弛一次，判断负环需要 n + 1
                if(!vis[v]) q.push(v), vis[v] = 1;
            }
        }
    }
    return false;
}

void solve(){
    cin >> n >> m;
    memset(h, -1, sizeof h);
    for(int i = 1; i <= m; i++){
        int a, b; double c;
        cin >> a >> b >> c;
        add(a, b, c);
    }
    for(int i = 1; i <= n; i++) add(0, i, 0);
    double l = -1e7, r = 1e7;
    while(r - l > 1e-10){
        double mid = (l + r) / 2;
        if(check(mid)) r = mid;
        else l = mid;
    }
    cout << fixed << setprecision(8) << r << endl;
}

signed main(){
    ios::sync_with_stdio(0), cin.tie(0), cout.tie(0);
    int T = 1;
    //cin >> T;
    while(T--) solve();
    return 0;
}
```